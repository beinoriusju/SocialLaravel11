<div class="d-inline">

          <!-- <span class="material-symbols-outlined position-relative">notifications -->
        @if ($unreadCount > 0)
            <span class="bg-primary badge badgenav text-white notification-badge">{{ $unreadCount }}</span>
        @endif
          <!-- </span> -->

</div>
